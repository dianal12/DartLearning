void main() {
  final name = 'Diana'; // we can add type but it's optional
  // final variables can only be set once and are
  // immutable
  // name = 'dana'; // can't change the value of a final variable
  print(name);
}
